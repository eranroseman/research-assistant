# Core dependencies
aiohttp>=3.10.0
click>=8.2.1
faiss-cpu>=1.12.0
numpy>=2.3.2
PyMuPDF>=1.26.3
psutil>=7.0.0
requests>=2.32.4
sentence-transformers>=5.1.0
tqdm>=4.67.1

# Dependencies for sentence-transformers
torch>=2.8.0
transformers>=4.55.2

# Section extraction dependencies
rapidfuzz>=3.0.0  # Fast fuzzy string matching for Tier 2 section detection
pdfplumber>=0.9.0  # PDF structure analysis for Tier 3 (optional but recommended)
pymupdf4llm>=0.0.5  # Enhanced markdown extraction from PDFs (optional but highly recommended)

# Performance optimization (optional but recommended for CPU inference)
# intel-extension-for-pytorch>=2.1.0  # Accelerates CPU inference by 2-3x on Intel CPUs
# Note: To install IPEX, uncomment above or run: pip install intel-extension-for-pytorch
